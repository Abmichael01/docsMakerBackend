"""
Realtime visitor activity is broadcast explicitly from the analytics views.

This module remains intentionally empty so the app import path stays stable
without duplicating websocket events on every VisitorLog save.
"""
